{
  "name": "ts-kafka-node",
  "version": "1.0.0",
  "description": "A Node.js project using TypeScript and Kafka.",
  "main": "dist/index.js",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start:dev": "ts-node src/index.ts",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "echo 'Running pre-build tasks...'",
    "postbuild": "echo 'Running post-build tasks...'"
  },
  "dependencies": {
    "@types/node": "^20.14.11",
    "dotenv": "^16.4.5",
    "kafkajs": "^2.2.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3",
    "zod": "^3.23.8"
  }
}